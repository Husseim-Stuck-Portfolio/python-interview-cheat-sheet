1. Basics & Variables

query | short explanation max one line | syntax example how to use it
variable assignment | Bind a value to a name | x = 42
multiple assignment | Assign multiple values at once | a, b, c = 1, 2, 3
chained assignment | Assign same value to multiple names | x = y = z = 0
swap variables | Exchange values without temp variable | a, b = b, a
unpack iterable | Spread values into variables | first, *rest = [1, 2, 3, 4]
ignore values | Use underscore to discard values | a, _, b = (1, 2, 3)
constant convention | Uppercase for constants (by convention) | MAX_SIZE = 100
print | Output to stdout | print("Hello", end="")
input | Read string from stdin | name = input("Name: ")
single-line comment | Ignore rest of line | # this is a comment
multi-line comment / docstring | String literal used as documentation | """Multi-line docstring"""
pass | Null operation / placeholder | def todo(): pass
del | Delete object/reference | del my_list[0]
None | Null/absence of value | result = None
assert | Debug assertion (raises AssertionError if False) | assert x > 0, "x must be positive"
breakpoint | Enter debugger (pdb) | breakpoint()


2. Operators

query | short explanation max one line | syntax example how to use it
+ addition | Sum two numbers or concatenate sequences | 5 + 3 → 8
- subtraction | Subtract right from left | 5 - 3 → 2
* multiplication | Product or sequence repetition | 5 * 3 or "ab" * 3
/ division | True division (always float) | 7 / 2 → 3.5
// floor division | Integer division (rounds down) | 7 // 2 → 3
% modulo | Remainder of division | 7 % 2 → 1
** exponentiation | Power operator | 2 ** 3 → 8
@ matrix mult | Matrix multiplication (PEP 465) | A @ B
+= augmented add | Add and assign in place | count += 1
-= augmented sub | Subtract and assign | count -= 1
*= augmented mul | Multiply and assign | total *= 2
/= augmented div | Divide and assign | total /= 2
//= augmented floor | Floor divide and assign | total //= 2
%= augmented mod | Modulo and assign | total %= 3
**= augmented pow | Exponentiate and assign | total **= 2
 | Value equality comparison | if a == b:
!= not equal | Value inequality | if a != b:
> greater than | Numeric/lexicographic comparison | if a > b:
< less than | Less than comparison | if a < b:
>= greater or equal | Greater than or equal | if a >= b:
<= less or equal | Less than or equal | if a <= b:
and logical AND | True if both operands are true | if a > 0 and b > 0:
or logical OR | True if at least one is true | if a > 0 or b > 0:
not logical NOT | Invert boolean value | if not valid:
is identity | Same object in memory | if x is None:
is not neg identity | Not the same object | if x is not None:
in membership | Value exists in container | if "a" in "abc":
not in neg membership | Value not in container | if "z" not in "abc":
& bitwise AND | Bit-level AND | 5 & 3 → 1
\| bitwise OR | Bit-level OR | 5 \| 3 → 7
^ bitwise XOR | Bit-level exclusive OR | 5 ^ 3 → 6
~ bitwise NOT | Invert all bits | ~5 → -6
<< left shift | Shift bits left | 1 << 3 → 8
>> right shift | Shift bits right | 8 >> 2 → 2
:= walrus operator | Assign within expression (PEP 572) | if (n := len(s)) > 5:
ternary conditional | Inline if-else expression | value = x if x > 0 else 0


3. Data Types & Conversion

query | short explanation max one line | syntax example how to use it
int | Integer (arbitrary precision) | x = 42
float | Floating-point number | y = 3.14
complex | Complex number | z = 3 + 4j
bool | Boolean (True/False) | flag = True
str | Unicode text string | s = "hello"
bytes | Immutable byte sequence | b = b"hello"
bytearray | Mutable byte sequence | ba = bytearray(b"hello")
type() | Get object type | type(x) → <class 'int'>
isinstance() | Check object type (preferred) | isinstance(x, (int, float))
int() | Convert to integer | int("42") or int("1010", 2)
float() | Convert to float | float("3.14")
str() | Convert to string | str(42)
bool() | Convert to boolean | bool(1) → True
list() | Convert to list | list("abc") → ['a','b','c']
tuple() | Convert to tuple | tuple([1, 2, 3])
set() | Convert to set | set([1, 2, 2]) → {1, 2}
dict() | Create dictionary from pairs | dict([('a', 1)])
frozenset() | Create immutable set | frozenset([1, 2])
bin() | Integer to binary string | bin(10) → '0b1010'
oct() | Integer to octal string | oct(10) → '0o12'
hex() | Integer to hex string | hex(10) → '0xa'
chr() | Unicode code point to character | chr(97) → 'a'
ord() | Character to Unicode code point | ord('a') → 97
bytes() | Create bytes object | bytes("hello", "utf-8")
bytearray() | Create bytearray object | bytearray(5)


4. Strings & Formatting

query | short explanation max one line | syntax example how to use it
string literal (single) | Single-quoted string | s = 'hello'
string literal (double) | Double-quoted string | s = "hello"
multi-line string | Triple-quoted multiline text | s = """line1\nline2"""
raw string | Ignore escape sequences | r"C:\Users\name"
f-string | Embedded expressions in string (PEP 498) | f"value is {x + 1}"
f-string debug | Print expression and value (PEP 701) | f"{x=}" → x=42
.format() | Positional/named formatting | "{} {}".format(a, b)
% formatting | Old-style C printf formatting | "%s %d" % (name, age)
string concatenation | Join strings with + | "Hello" + " " + "World"
string repetition | Repeat with * | "-" * 10
string slicing | Extract substring by index | s[1:4] or s[::-1]
.upper() | Convert to uppercase | "hello".upper()
.lower() | Convert to lowercase | "HELLO".lower()
.title() | Title case each word | "hello world".title()
.strip() | Remove leading/trailing whitespace | "  hello  ".strip()
.lstrip() / .rstrip() | Remove left/right whitespace | "  hello  ".lstrip()
.split() | Split into list by delimiter | "a,b,c".split(",")
.rsplit() | Split from the right | "a,b,c".rsplit(",", 1)
.join() | Join iterable with separator | ",".join(["a", "b", "c"])
.replace() | Replace substring | "hello".replace("l", "z")
.find() | Find substring index (or -1) | "hello".find("ll") → 2
.index() | Find substring index (or ValueError) | "hello".index("ll")
.count() | Count occurrences | "hello".count("l") → 2
.startswith() | Check prefix | "hello".startswith("he")
.endswith() | Check suffix | "hello".endswith("lo")
.removeprefix() | Remove prefix if present (3.9+) | "TestHook".removeprefix("Test")
.removesuffix() | Remove suffix if present (3.9+) | "TestHook".removesuffix("Hook")
.isdigit() | All characters are digits | "123".isdigit()
.isalpha() | All characters are alphabetic | "abc".isalpha()
.isspace() | All characters are whitespace | "  ".isspace()
.zfill() | Pad with zeros on left | "42".zfill(5) → "00042"
.ljust() / .rjust() | Left/right justify with padding | "hi".ljust(5, "-")


5. Data Structures (Collections)

query | short explanation max one line | syntax example how to use it
list literal | Ordered mutable sequence | lst = [1, 2, 3]
list indexing | Access item by index | lst[0] or lst[-1]
list slicing | Extract sublist | lst[1:3] or lst[::2]
list assignment | Modify item by index | lst[0] = 99
list append() | Add item to end | lst.append(4)
list extend() | Append all items from iterable | lst.extend([5, 6])
list insert() | Insert at specific index | lst.insert(1, "x")
list remove() | Remove first matching value | lst.remove(2)
list pop() | Remove and return item by index | lst.pop() or lst.pop(0)
list clear() | Remove all items | lst.clear()
list index() | Find index of value | lst.index(3)
list count() | Count occurrences | lst.count(2)
list sort() | Sort list in place | lst.sort(reverse=True)
list reverse() | Reverse list in place | lst.reverse()
list copy() | Shallow copy of list | lst.copy() or lst[:]
tuple literal | Ordered immutable sequence | t = (1, 2, 3) or t = 1, 2
tuple unpacking | Extract values into variables | a, b = (1, 2)
namedtuple | Tuple with named fields | from collections import namedtuple; Point = namedtuple("Point", ["x", "y"])
dict literal | Key-value mapping | d = {"a": 1, "b": 2}
dict access by key | Get value (raises KeyError if missing) | d["a"]
dict .get() | Get value with default fallback | d.get("c", 0)
dict .setdefault() | Set key if missing, then return value | d.setdefault("c", 0)
dict .keys() | View of all keys | d.keys()
dict .values() | View of all values | d.values()
dict .items() | View of (key, value) pairs | d.items()
dict .update() | Merge another mapping | d.update({"c": 3})
dict .pop() | Remove key and return value | d.pop("a")
dict .popitem() | Remove and return last inserted item | d.popitem()
dict clear() | Remove all items | d.clear()
set literal | Unordered unique elements | s = {1, 2, 3}
set add() | Add element | s.add(4)
set remove() | Remove element (raises KeyError) | s.remove(2)
set discard() | Remove if present (no error) | s.discard(99)
set pop() | Remove and return arbitrary element | s.pop()
set union() | Combine sets (or \|) | s1 \| s2 or s1.union(s2)
set intersection() | Common elements (or &) | s1 & s2
set difference() | In first but not second (or -) | s1 - s2
set symmetric_difference() | In either but not both (or ^) | s1 ^ s2
frozenset | Immutable set (hashable) | fs = frozenset([1, 2])
range() | Sequence of numbers | range(0, 10, 2)
enumerate() | Index-value pairs from iterable | enumerate(["a", "b"], start=1)
zip() | Aggregate elements from iterables | zip([1,2], ["a","b"])
zip() with strict | Error if lengths differ (3.10+) | zip(a, b, strict=True)


6. Control Flow

query | short explanation max one line | syntax example how to use it
if statement | Conditional execution | if x > 0: print("positive")
elif statement | Alternative condition | elif x == 0: print("zero")
else statement | Default block | else: print("negative")
match / case | Structural pattern matching (3.10+) | match value: case 1: ...
match literal | Match exact value | case 200:
match variable | Capture matched value | case status:
match OR pattern | Match multiple values | case 401 \| 403 \| 404:
match sequence | Match list/tuple structure | case [x, y]:
match mapping | Match dict structure | case {"name": str(n)}:
match class pattern | Match object type and attrs | case Point(x=0, y=0):
match wildcard | Catch-all pattern | case _:
match guard | Additional condition on case | case n if n > 0:
for loop | Iterate over sequence | for item in items: print(item)
while loop | Loop while condition is true | while x < 10: x += 1
break | Exit loop immediately | for x in lst: if x < 0: break
continue | Skip to next iteration | for x in lst: if x == 0: continue
pass | Empty loop body placeholder | for x in lst: pass
for-else | Execute if loop didn’t break | for x in lst: ... else: ...
while-else | Execute if while didn’t break | while cond: ... else: ...


7. Comprehensions

query | short explanation max one line | syntax example how to use it
list comprehension | Build list from expression | [x**2 for x in range(5)]
list comp with filter | List comp with condition | [x for x in lst if x > 0]
list comp with if-else | Conditional expression inside | ["even" if x%2==0 else "odd" for x in range(5)]
dict comprehension | Build dict from expression | {k: v for k, v in zip(keys, vals)}
dict comp with filter | Dict comp with condition | {k: v for k, v in d.items() if v > 0}
set comprehension | Build set from expression | {x % 3 for x in range(10)}
generator expression | Lazy evaluation iterable | (x**2 for x in range(1000000))
nested comprehension | Multi-level iteration | [(x, y) for x in range(3) for y in range(3)]
walrus in comprehension | Assign and filter in one | [y for x in data if (y := func(x)) > 0]


8. Functions

query | short explanation max one line | syntax example how to use it
def | Define a function | def greet(name): return f"Hi {name}"
return | Return value from function | return x + y
return multiple | Return tuple (multiple values) | return a, b
default argument | Parameter with default value | def greet(name="World"): ...
positional-only args | Args before / are positional-only | def f(a, b, /, c): ...
keyword-only args | Args after * are keyword-only | def f(*, a, b): ...
*args | Variable positional arguments | def f(*args): return sum(args)
**kwargs | Variable keyword arguments | def f(**kwargs): print(kwargs)
combined signature | Positional, args, keyword, kwargs | def f(a, /, b, *args, c, **kwargs):
lambda | Anonymous inline function | square = lambda x: x ** 2
type hints (basic) | Annotate parameter/return types | def add(x: int, y: int) -> int:
docstring | Function documentation string | def f():\n    """Does thing."""
yield | Produce generator value | def gen(): yield 1; yield 2
yield from | Delegate to sub-generator | yield from other_generator()
return in generator | StopIteration with value (3.3+) | return "done" inside generator
nested function | Function inside function | def outer(): def inner(): ...
closure | Function remembers enclosing scope | def make_adder(n): return lambda x: x+n
functools.partial | Fix some function arguments | from functools import partial; base2 = partial(int, base=2)
@overload | Multiple type signatures (typing) | @overload def f(x: int) -> int: ...


9. Classes & OOP

query | short explanation max one line | syntax example how to use it
class | Define a class | class Person: ...
__init__ | Constructor / initializer | def __init__(self, name): self.name = name
self | Instance reference (first param convention) | def speak(self): print(self.name)
instance variable | Attribute unique to instance | self.age = 30
class variable | Attribute shared across instances | species = "Homo sapiens"
instance method | Regular method operating on self | def greet(self): ...
@classmethod | Method receiving class as first arg | @classmethod def create(cls, name): ...
@staticmethod | Method with no self/class arg | @staticmethod def is_adult(age): ...
@property | Getter for attribute-like access | @property def name(self): return self._name
@name.setter | Setter for property | @name.setter def name(self, val): self._name = val
@name.deleter | Deleter for property | @name.deleter def name(self): del self._name
inheritance | Derive from parent class | class Student(Person): ...
super() | Call parent class method | super().__init__(name)
multiple inheritance | Inherit from multiple bases | class C(A, B): ...
MRO | Method Resolution Order | ClassName.__mro__
__str__ | Human-readable string representation | def __str__(self): return self.name
__repr__ | Unambiguous/debug representation | def __repr__(self): return f"Person({self.name!r})"
__eq__ | Equality comparison | def __eq__(self, other): return self.id == other.id
__lt__ | Less-than comparison | def __lt__(self, other): return self.age < other.age
__len__ | Support len() | def __len__(self): return len(self.data)
__getitem__ | Support indexing [] | def __getitem__(self, idx): return self.data[idx]
__setitem__ | Support item assignment | def __setitem__(self, idx, val): self.data[idx] = val
__contains__ | Support in operator | def __contains__(self, item): return item in self.data
__call__ | Make instance callable | def __call__(self): return self.value
__iter__ / __next__ | Make object iterable/iterator | def __iter__(self): ...
@dataclass | Auto-generate init/repr/eq (3.7+) | @dataclass class Point: x: float; y: float
dataclass frozen | Immutable dataclass | @dataclass(frozen=True)
ClassVar | Class-level variable in dataclass | MY_STATIC: ClassVar[int] = 42
__slots__ | Restrict attributes, save memory | __slots__ = ("x", "y")
abstract base class | Enforce method implementation | from abc import ABC, abstractmethod
metaclass | Class of a class | class Meta(type): ...


10. Error Handling

query | short explanation max one line | syntax example how to use it
try / except | Catch exceptions | try: 1/0 except ZeroDivisionError: ...
except as | Catch and bind exception instance | except ValueError as e: print(e)
except multiple | Catch multiple exception types | except (ValueError, TypeError): ...
else | Run if no exception occurred | else: print("success")
finally | Always execute cleanup block | finally: file.close()
raise | Manually raise exception | raise ValueError("bad input")
raise from | Chain exceptions explicitly | raise CustomError() from e
custom exception | User-defined exception class | class ValidationError(Exception): pass
ExceptionGroup | Group multiple exceptions (3.11+) | raise ExceptionGroup("group", [e1, e2])
except* | Catch from ExceptionGroup (3.11+) | except* ValueError: ...
warnings.warn | Non-fatal warning | import warnings; warnings.warn("deprecated")


11. File I/O

query | short explanation max one line | syntax example how to use it
open() | Open file and return file object | f = open("data.txt", "r", encoding="utf-8")
with open() | Context manager auto-closes file | with open("file.txt") as f: data = f.read()
read() | Read entire file content | content = f.read()
readline() | Read single line | line = f.readline()
readlines() | Read all lines into list | lines = f.readlines()
write() | Write string to file | f.write("hello\n")
writelines() | Write list of strings | f.writelines(["a\n", "b\n"])
seek() | Change file position | f.seek(0)
tell() | Get current file position | pos = f.tell()
pathlib.Path | Object-oriented filesystem paths | from pathlib import Path; p = Path("file.txt")
Path.read_text() | Read file via pathlib | Path("file.txt").read_text(encoding="utf-8")
Path.write_text() | Write file via pathlib | Path("file.txt").write_text("hello")
json.load() | Parse JSON from file | import json; data = json.load(f)
json.dump() | Serialize to JSON file | json.dump(data, f, indent=2)
json.loads() | Parse JSON from string | data = json.loads('{\"a\": 1}')
json.dumps() | Serialize to JSON string | s = json.dumps(data)
csv.reader() | Read CSV file | import csv; rows = list(csv.reader(f))
csv.DictReader() | Read CSV as dictionaries | rows = list(csv.DictReader(f))
csv.writer() | Write CSV file | w = csv.writer(f); w.writerow([1, 2])


12. Built-in Functions (Essential)

query | short explanation max one line | syntax example how to use it
abs() | Absolute value | abs(-5) → 5
all() | True if all elements are true | all([1, 1, 0]) → False
any() | True if any element is true | any([0, 0, 1]) → True
ascii() | ASCII representation with escapes | ascii("é") → '\xe9'
callable() | Check if object is callable | callable(print) → True
compile() | Compile source into code object | compile("1+2", "", "eval")
divmod() | Quotient and remainder | divmod(17, 5) → (3, 2)
eval() | Evaluate string as expression | eval("1 + 2")
exec() | Execute string as statement | exec("x = 1")
filter() | Filter iterable by function | filter(lambda x: x>0, nums)
format() | Format value | format(3.14159, ".2f")
getattr() | Get attribute by name string | getattr(obj, "name")
hasattr() | Check if attribute exists | hasattr(obj, "name")
setattr() | Set attribute by name string | setattr(obj, "name", "val")
delattr() | Delete attribute by name string | delattr(obj, "name")
globals() | Dictionary of global namespace | globals()
locals() | Dictionary of local namespace | locals()
vars() | __dict__ of object or locals() | vars(obj)
hash() | Compute hash value | hash("hello")
help() | Invoke built-in help system | help(str.split)
id() | Return object identity (memory address) | id(x)
len() | Return length of object | len("hello") → 5
map() | Apply function to every item | map(str, [1, 2, 3])
max() | Largest item | max([1, 5, 3]) or max(lst, key=len)
min() | Smallest item | min([1, 5, 3]) or min(lst, key=len)
pow() | Power / modular exponent | pow(2, 3) or pow(2, 3, 5)
repr() | Official string representation | repr(obj)
reversed() | Reverse iterator | reversed([1, 2, 3])
round() | Round to nearest (ties to even) | round(2.5) → 2
sorted() | Return sorted list | sorted([3, 1, 2], reverse=True)
sum() | Sum of iterable | sum([1, 2, 3])
zip() | Pairwise iteration | list(zip(a, b))


13. Modules & Imports

query | short explanation max one line | syntax example how to use it
import module | Import entire module | import os
from module import name | Import specific name | from math import sqrt
from module import * | Import all public names | from math import *
import as | Alias module/name | import numpy as np
relative import (same pkg) | Import from same package | from . import module
relative import (parent) | Import from parent package | from .. import module
__name__ == "__main__" | Guard for script execution | if __name__ == "__main__": main()
__all__ | Control from module import * | __all__ = ["foo", "bar"]
sys.path | List of module search paths | import sys; print(sys.path)
dir() | List names in module/scope | dir(math)
__import__() | Programmatic import | mod = __import__("os")


14. Iterators & Generators

query | short explanation max one line | syntax example how to use it
iter() | Get iterator from iterable | it = iter([1, 2, 3])
next() | Get next item from iterator | next(it) or next(it, default)
yield | Produce value, pause generator | def gen(): yield 1
yield from | Delegate iteration | yield from range(5)
generator expression | Lazy one-shot iterator | gen = (x*2 for x in range(10))
itertools.count | Infinite counter | itertools.count(start=10, step=2)
itertools.cycle | Infinite cycle over iterable | itertools.cycle("ABC")
itertools.repeat | Repeat value N times | itertools.repeat(5, 3)
itertools.chain | Flatten multiple iterables | itertools.chain(a, b, c)
itertools.islice | Slice iterator lazily | itertools.islice(gen, 5)
itertools.groupby | Group consecutive equal items | itertools.groupby(sorted(data))
itertools.permutations | All r-length permutations | itertools.permutations("ABC", 2)
itertools.combinations | All r-length combinations | itertools.combinations("ABC", 2)
reversed() | Reverse iterator | reversed([1, 2, 3])


15. Context Managers

query | short explanation max one line | syntax example how to use it
with statement | Context manager protocol | with open("file.txt") as f: ...
multiple contexts | Nested context managers (3.1+) | with A() as a, B() as b: ...
__enter__ | Enter context (setup) | def __enter__(self): return self
__exit__ | Exit context (teardown) | def __exit__(self, exc_type, exc, tb): ...
@contextmanager | Decorator to build context manager | @contextmanager def managed(): ...
contextlib.suppress | Ignore specific exceptions | with suppress(FileNotFoundError): ...
contextlib.closing | Ensure .close() is called | with closing(urllib.urlopen(url)) as page:


16. Type Hints & Typing

query | short explanation max one line | syntax example how to use it
basic type hint | Annotate variable type | name: str = "Alice"
function type hints | Annotate params and return | def f(x: int) -> str: ...
Union (old) | Multiple possible types | Union[int, str]
union with \| (3.10+) | Cleaner union syntax | int \| str
Optional (old) | Type or None | Optional[int]
optional with \| None | Cleaner optional syntax (3.10+) | int \| None
list[int] | Generic builtin (3.9+) | def f(items: list[int]) -> None:
dict[str, int] | Generic dict (3.9+) | scores: dict[str, int] = {}
tuple[int, str] | Fixed-size tuple types | def point() -> tuple[int, int]:
Callable | Function type | Callable[[int, int], int]
Any | Opt-out of type checking | from typing import Any; x: Any = 1
TypeVar | Generic type variable | T = TypeVar("T")
TypeVar with bound | Constrain to subtype | T = TypeVar("T", bound=int)
TypeVar with default (3.13+) | Default type for TypeVar | T = TypeVar("T", default=int)
Generic | User-defined generic class | class Stack(Generic[T]): ...
Protocol | Structural subtyping | class Drawable(Protocol): ...
TypedDict | Dict with specific key types | class Movie(TypedDict): name: str
Unpack | Unpack TypedDict into kwargs | def f(**kwargs: Unpack[Movie]):
Self (3.11+) | Return type is same class | from typing import Self; def clone(self) -> Self:
Literal | Specific literal values | mode: Literal["r", "w"] = "r"
Final | Prevent reassignment | MAX: Final[int] = 100
ClassVar | Class-level variable | count: ClassVar[int] = 0
TypeAlias (old) | Define type alias | Vector = list[float]
type statement (3.12+) | New type alias syntax | type Vector = list[float]
generic function (3.12+) | Type params on function | def first[T](items: list[T]) -> T:
generic class (3.12+) | Type params on class | class Box[T]: ...
@overload | Multiple type signatures | @overload def process(x: int) -> int: ...
reveal_type() | Debug inferred type (checker) | reveal_type(x)
@deprecated (3.13+) | Mark deprecated functions | from warnings import deprecated; @deprecated("use new()")


17. Decorators & Functional

query | short explanation max one line | syntax example how to use it
@decorator | Wrap function with another | @timer def slow(): ...
decorator with args | Decorator factory | @repeat(3) def greet(): ...
functools.wraps | Preserve metadata of wrapped func | @wraps(func) def wrapper(): ...
functools.lru_cache | Memoization cache | @lru_cache(maxsize=128)
functools.cache | Unbounded cache (3.9+) | @cache
functools.partial | Pre-fill function arguments | base2 = partial(int, base=2)
functools.reduce | Cumulative reduction | reduce(lambda a,b: a+b, [1,2,3])
functools.singledispatch | Function overloading by type | @singledispatch def process(arg): ...
@cached_property | Cache property value (3.8+) | @cached_property def area(self): ...


18. Advanced / Modern Syntax

query | short explanation max one line | syntax example how to use it
match class with guard | Pattern match object + condition | case Point(x, y) if x > 0:
match sequence wildcard | Capture rest of sequence | case [first, *rest]:
match mapping wildcard | Capture remaining keys | case {"name": n, **rest}:
match star pattern | Capture multiple in class pattern | case Point(*coords):
exception notes (3.11+) | Add notes to exceptions | e.add_note("Extra context")
tomllib (3.11+) | Parse TOML files | import tomllib; tomllib.load(f)
typing.cast | Force type in checker | from typing import cast; cast(str, x)
__init__.py optional | Implicit namespace packages (PEP 420) | Package folders work without __init__.py
__future__ annotations | Postpone annotation evaluation | from __future__ import annotations
